% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buzzEBMcontMcontY.R
\name{buzzEBMcontMcontY}
\alias{buzzEBMcontMcontY}
\title{Select continuous mediators using the GT-exploratory Bayesian mediation model with continuous dependent variables.}
\usage{
buzzEBMcontMcontY(
  model,
  dataset,
  my_prior = NULL,
  advanced = NULL,
  m.prec.shape = NULL,
  m.prec.rate = NULL,
  y.prec.shape = NULL,
  y.prec.rate = NULL,
  a.pip.hyperalpha = NULL,
  a.pip.hyperbeta = NULL,
  b.pip.hyperalpha = NULL,
  b.pip.hyperbeta = NULL,
  direct.coef.mean = NULL,
  direct.coef.precision = NULL,
  m.prec.init = NULL,
  y.prec.init = NULL,
  direct.coef.init = NULL,
  a.pip.hyperprior.init = NULL,
  b.pip.hyperprior.init = NULL,
  n_chains = NULL,
  n_adapt = NULL,
  n_burnin = NULL,
  n_iter = NULL,
  thin = NULL
)
}
\arguments{
\item{model}{A character string specifying the mediation model to be
fitted. The model should be specified using the syntax
\code{"Y ~ X1 + X2 | M1 + M2"}, where \code{Y}, \code{X1},
\code{X2}, \code{M1}, and \code{M2} should be replaced by the names of the
corresponding variables in the dataset.}

\item{dataset}{A \code{data.frame} containing the variables specified in the model.}

\item{my_prior}{Optional \code{data.frame} containing custom prior specifications.
Run \code{parms <- run_parms_wizard()} to see the required structure.}

\item{advanced}{Character. Use \code{"interactive"} for an interactive wizard
to choose parameter distributions, or leave \code{NULL} for defaults.}

\item{m.prec.shape}{Numeric scalar or vector of length equal to the number
of mediators. Shape hyperparameter of the Gamma prior distribution for the
mediator residual precisions. The default value is 1.}

\item{m.prec.rate}{Numeric scalar or vector of length equal to the number
of mediators. Rate hyperparameter of the Gamma prior distribution for the
mediator residual precisions. The default value is 0.001.}

\item{y.prec.shape}{Numeric scalar. Shape hyperparameter of the Gamma prior
distribution for the outcome residual precision. The default value is 1.}

\item{y.prec.rate}{Numeric scalar. Rate hyperparameter of the Gamma prior
distribution for the outcome residual precision. The default value is
0.001.}

\item{a.pip.hyperalpha}{Numeric scalar or vector. Alpha hyperparameter of
the Beta prior distribution for the \eqn{a}-path inclusion probabilities.
The default value is 3.}

\item{a.pip.hyperbeta}{Numeric scalar or vector. Beta hyperparameter of
the Beta prior distribution for the \eqn{a}-path inclusion probabilities.
The default value is 3.}

\item{b.pip.hyperalpha}{Numeric scalar or vector. Alpha hyperparameter of
the Beta prior distribution for the \eqn{b}-path inclusion probabilities.
The default value is 3.}

\item{b.pip.hyperbeta}{Numeric scalar or vector. Beta hyperparameter of
the Beta prior distribution for the \eqn{b}-path inclusion probabilities.
The default value is 3.}

\item{direct.coef.mean}{Numeric scalar or vector. Mean parameter of the
Normal prior distribution for the direct effects (\eqn{c'}). The default
value is 0.}

\item{direct.coef.precision}{Numeric scalar or vector. Precision parameter
of the Normal prior distribution for the direct effects (\eqn{c'}). The
default value is 1.0E-6.}

\item{m.prec.init}{Numeric or \code{NULL}. Initial value for the mediator
residual precision. The default value is 1.}

\item{y.prec.init}{Numeric or \code{NULL}. Initial value for the outcome
residual precision. The default value is 1.}

\item{direct.coef.init}{Numeric or \code{NULL}. Initial value for the direct
effect (\eqn{c'}). Default is 0.}

\item{a.pip.hyperprior.init}{Numeric or \code{NULL}. Initial value of the
\eqn{a}-path inclusion probability (PIP). The default value is 0.5.}

\item{b.pip.hyperprior.init}{Numeric or \code{NULL}. Initial value of the
\eqn{b}-path inclusion probability (PIP). The default value is 0.5.}

\item{n_chains}{Integer. Number of MCMC chains.}

\item{n_adapt}{Integer. Number of adaptation iterations.}

\item{n_burnin}{Integer. Number of burn-in iterations.}

\item{n_iter}{Integer. Number of post-burn-in iterations.}

\item{thin}{Integer. Thinning interval.}
}
\value{
A matrix of posterior summary statistics for the monitored
parameters.
}
\description{
This function selects time-invariant continuous mediators using the generalized
two-stage exploratory Bayesian mediation model with continuous dependent variables.
}
\details{
Fits a Bayesian mediation model specifically designed for cases where both
the mediators (\eqn{M}) and the outcome variable (\eqn{Y}) are continuous.
}
\examples{
# 1. Create a continuous synthetic dataset
set.seed(123)
n <- 100
toy_data <- data.frame(
   X = rnorm(n),
   M1 = rnorm(n),
   M2 = rnorm(n),
   Y = rnorm(n)
)

# 2. Fit the model using lavaan-style syntax
# We define Y as the outcome, with M1, M2, and X as predictors
results <- buzzEBMcontMcontY(
   model    = "Y ~ X | M1 + M2",
   dataset  = toy_data,
   n_burnin = 500,
   n_iter   = 2000
)

# 3. Check results
summary(results)

}
\references{
Shi, D., Shi, D., and Fairchild, A. J. (2023) "Variable Selection for Mediators
under a Bayesian Mediation Model" <doi:10.1080/10705511.2022.2164285>
}
