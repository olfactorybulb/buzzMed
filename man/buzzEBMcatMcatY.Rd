% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buzzEBMcatMcatY.R
\name{buzzEBMcatMcatY}
\alias{buzzEBMcatMcatY}
\title{Fit Bayesian Mediation Model (Binary M & Binary Y)}
\usage{
buzzEBMcatMcatY(
  model,
  dataset,
  my_prior = NULL,
  advanced = NULL,
  a.coef.mean = NULL,
  a.coef.prec = NULL,
  b.coef.mean = NULL,
  b.coef.prec = NULL,
  a.pip.hyperalpha = NULL,
  a.pip.hyperbeta = NULL,
  b.pip.hyperalpha = NULL,
  b.pip.hyperbeta = NULL,
  direct.coef.mean = NULL,
  direct.coef.precision = NULL,
  direct.coef.init = NULL,
  a.pip.hyperprior.init = NULL,
  b.pip.hyperprior.init = NULL,
  n_chains = NULL,
  n_adapt = NULL,
  n_burnin = NULL,
  n_iter = NULL,
  thin = NULL
)
}
\arguments{
\item{model}{A description of the model to be fitted. This is typically a
formula or a character string using \code{lavaan} syntax (e.g., \code{Y ~ M + X}).}

\item{dataset}{A \code{data.frame} containing the variables specified in the model.}

\item{my_prior}{Optional \code{data.frame} containing custom prior specifications.
Run \code{parms <- run_parms_wizard()} to see the required structure.}

\item{advanced}{Character. Use \code{"interactive"} for an interactive wizard
to choose parameter distributions, or leave \code{NULL} for defaults.}

\item{a.coef.mean, a.coef.prec}{Numeric scalar or vector.
Mean and precision parameters of the Normal prior for the \eqn{a} path effects.
By default, a Normal distribution is used. The default values are 0 and 1.0E-6,
respectively.}

\item{b.coef.mean, b.coef.prec}{Numeric scalar or vector.
Mean and precision parameters of the Normal prior for the \eqn{b} path effects.
By default, a Normal distribution is used. The default values are 0 and 1.0E-6,
respectively.}

\item{a.pip.hyperalpha, a.pip.hyperbeta}{Numeric scalar or vector. Alpha and
beta parameters for the Beta hyperprior of the \eqn{a} path inclusion
probabilities. By default, a Beta distribution is used. The default value is 3.}

\item{b.pip.hyperalpha, b.pip.hyperbeta}{Numeric scalar or vector. Alpha and
beta parameters for the Beta hyperprior of the \eqn{b} path inclusion
probabilities. By default, a Beta distribution is used. The default value is 3.}

\item{direct.coef.mean, direct.coef.precision}{Numeric scalar or vector.
Mean and precision parameters for the Normal prior of the direct effects
(\eqn{c'}). By default, a Normal distribution is used. The default values
are 0 and 1.0E-6, respectively.}

\item{direct.coef.init}{Numeric or \code{NULL}. Initial value for the direct
effect (\eqn{c'}). Default is 0.}

\item{a.pip.hyperprior.init, b.pip.hyperprior.init}{Numeric or \code{NULL}.
Initial inclusion probabilities (PIP). Default is 0.5.}

\item{n_chains}{Integer. Number of MCMC chains.}

\item{n_adapt}{Integer. Number of adaptation iterations.}

\item{n_burnin}{Integer. Number of burn-in iterations.}

\item{n_iter}{Integer. Number of post-burn-in iterations.}

\item{thin}{Integer. Thinning interval.}
}
\value{
An object of class \code{mcmc.list} containing posterior samples.
}
\description{
This function implements variable selection for mediators when dealing with
binary data. It utilizes a latent probit link formulation to model the
probabilities of the binary responses within the JAGS framework. It automates
data preparation, JAGS model construction, and MCMC sampling.
}
\details{
Fits a Bayesian mediation model specifically designed for cases where both
the mediators (\eqn{M}) and the outcome variable (\eqn{Y}) are binary (0/1).


Since both \eqn{M} and \eqn{Y} are binary, residual precisions are not
estimated (they are fixed to 1 in the latent probit space).
This function identifies \eqn{X, M, Y} via \code{.parse_buzz_syntax}
and routes the data through an internal pipeline including
\code{\link{prepare_ebmed_data}} and \code{build_ebmed_model_mcat_ycat}.
}
\note{
This function is strictly for binary data. For variables with more than
two levels, please pre-process them into dummy variables or ensure they
are binary before running.
}
\examples{
\dontrun{
# Binary case: Both M and Y are Binary
set.seed(101)
n <- 100
toy_data <- data.frame(
   X = rbinom(n, 1, 0.5),
   M1 = rbinom(n, 1, 0.3),
   M2 = rbinom(n, 1, 0.7),
   Y = rbinom(n, 1, 0.5)
)

# Fit the model
results <- buzzEBMcatMcatY(
   model    = "Y ~ M1 + M2 | X",
   dataset  = toy_data,
   n_burnin = 200,
   n_iter   = 1000
)

summary(results)
}

}
\references{
Shi, D., Shi, D., & Fairchild, A. J. (2023). Variable Selection for Mediators
under a Bayesian Mediation Model. \emph{Structural Equation Modeling: A
Multidisciplinary Journal}, 30(6), 887–900.
\doi{10.1080/10705511.2022.2164285}
}
\seealso{
Other buzzEBMed_fitters: 
\code{\link{buzzEBMcatMcontY}()},
\code{\link{buzzEBMcontMcatY}()},
\code{\link{buzzEBMcontMcontY}()}
}
\concept{buzzEBMed_fitters}
